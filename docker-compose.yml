services:
  bot:
    image: ghcr.io/yevhen-lomakin/wateralin:latest
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - DATABASE_PATH=/app/data/water_tracker.db
    volumes:
      - bot-data:/app/data

volumes:
  bot-data:
